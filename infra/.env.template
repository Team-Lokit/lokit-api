# Deploy (docker-compose app image tag)
APP_IMAGE_TAG=prod

# Database
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Spring
SPRING_PROFILES_ACTIVE=

# JWT
JWT_SECRET=
JWT_EXPIRATION=
JWT_REFRESH_EXPIRATION=

# AWS
AWS_REGION=
AWS_S3_BUCKET=

# CORS
CORS_ALLOWED_ORIGINS=

# FCM push (Firebase 서비스 계정 JSON은 APPLE_KEY_HOST_DIR과 같은 시크릿 디렉터리에 넣고
# file: 접두사로 경로를 지정한다 — 예: file:/app/secrets/firebase-service-account.json)
NOTIFICATION_PUSH_ENABLED=false
NOTIFICATION_PUSH_FCM_PROJECT_ID=
NOTIFICATION_PUSH_FCM_CREDENTIALS_LOCATION=
